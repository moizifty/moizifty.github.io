---
title: Geo
order: 9
---

`src/geo` is a stub right now — `geoCore.h` only pulls in the math,
memory, thread, and string headers it'll build on and doesn't define any
types or functions yet. Placeholder page until there's real geometry code
here (meshes, collision, spatial structures).
