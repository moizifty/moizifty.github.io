---
title: Geo
order: 10
---

`src/geo` is a stub right now, `geoCore.h` just pulls in the math, memory, thread and string headers it'll eventually build on and doesn't actually define anything yet. Nothing else in the codebase touches it either. Keeping this page around as a placeholder for whenever I get round to writing real geometry code here, meshes, collision, spatial structures, that kind of thing.
